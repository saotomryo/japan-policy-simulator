# 国版 政策シミュレーター 画面検討メモ

## 1. 画面設計の前提

既存の静的Webアプリ構成を活かし、MVPでは複雑なルーティングを追加しない。

画面は `src/app.js` の `activeView` による切り替えを継続する。既存の学園版画面を国版へ置き換える。

```text
dashboard -> ダッシュボード
voices    -> 声の分析
issues    -> 政策ターゲット
policy    -> 政策案・分析チャット
result    -> 実行結果
save      -> 保存モーダル
```

MVPでは、最初から画面数を増やしすぎない。政策ターゲット指定、声の分析、政策案、実行結果の4つを中心にし、保存とAI設定は既存モーダルを流用する。

指標や効果表示は固定リストにしない。政策ごとに関連する指標、セグメント、国際関係クラスター、効果軸が変わるため、画面上では分析ビューを切り替えられるようにする。

### 動的表示の基本方針

- AIが政策ターゲットごとに `relatedMetricIds` と `recommendedViews` を返す
- ダッシュボードは全体指標を持つが、政策選択後は関連指標を優先表示する
- 声の分析、政策案、実行結果では、世代別、所得別、地域別、産業別、国際関係、財政影響などをタブまたはセグメントで切り替える
- 政策との関連が薄い効果軸は、数値を無理に表示せず `該当性が低い` と理由を表示する
- 画面ごとの初期表示はAIの `recommendedViews` の先頭を使う
- ユーザーは表示軸を手動で切り替えられる

## 2. ナビゲーション

### MVPナビ

- ダッシュボード
- 政策ターゲット
- 声の分析
- 政策案
- 実行結果
- 保存
- AI設定

### 表示順

```text
ダッシュボード
  ↓
政策ターゲット
  ↓
声の分析
  ↓
政策案
  ↓
実行結果
```

ただし、ユーザーは各画面を自由に戻って確認できる。実行後のみ、政策ターゲットと政策案の編集操作をロックする。

### 既存画面との対応

| 既存画面 | 国版MVP画面 | 変更内容 |
| --- | --- | --- |
| ダッシュボード | ダッシュボード | 学園指標を日本の政策指標へ差し替え |
| 声の分析 | 声の分析 | 生徒の声を国民・ステークホルダーの声へ差し替え |
| 課題設定 | 政策ターゲット | 課題候補選択を廃止し、政策指定へ置換 |
| 施策検討 | 政策案 | 政策案と政策分析チャットを統合 |
| 結果 | 実行結果 | 年度末ではなく政策実行結果へ置換 |
| 保存 | 保存 | IndexedDB / JSON保存を流用 |

## 3. ダッシュボード画面

### 目的

政策を決める前に、日本の初期状態とシミュレーション前提を把握する。

### 主要領域

1. ヘッダーサマリー
2. 社会・経済・財政指標
3. 国際関係スコア
4. 人口構成サマリー
5. データ出典・データ種別

### 表示項目

#### ヘッダーサマリー

- シナリオ名
- データ基準年
- 政策実行状態
  - 未選択
  - 分析中
  - 政策案作成済み
  - 実行済み
- 現在選択中の政策名

#### 指標カード

カテゴリ別に表示する。ただし、政策選択後は固定順ではなく、選択政策に関連する指標を上位に表示する。

- 社会指標
- 経済・財政指標
- 政策実行指標
- 国際関係指標

各カードに表示する。

- 指標名
- 値
- 単位
- データ種別
- 基準年
- 簡単な説明

データ種別は視覚的に区別する。

- 実統計値
- 実統計参考値
- 仮想指標
- AI推定

政策選択後の表示モード。

- 全体
- 関連指標
- 財政
- 国際関係
- 社会影響
- 実装リスク

`関連指標` は、AIが返す `relatedMetricIds` を使って自動構成する。政策に関係しない指標は、折りたたみまたは `全体` 表示に回す。

#### 国際関係スコア

横並びまたは小型テーブルで表示する。

- アメリカ
- EU
- 中国
- アジア
- グローバルサウス
- 国際機関

各クラスターに表示する。

- 関係スコア
- 経済依存度
- 安全保障感度
- サプライチェーン重要度
- 政策反応メモ

#### 人口構成サマリー

MVPではグラフを3つに絞る。

- 世代構成
- 所得階層
- 地域構成

政策効果分析の土台であることが分かるよう、声の分析や実行結果の世代別・所得別効果と同じセグメントIDを使う。

### 主な操作

- `AIで初期化`
- `政策ターゲットへ進む`
- `詳細データを見る`

### MVPで後回し

- 実統計値のオンライン更新
- 出典URLの詳細ビュー
- 指標の手動編集

## 4. 政策ターゲット画面

### 目的

シミュレーション対象の政策を選ぶ、または自由記述から作る。

### 主要領域

1. プリセット政策選択
2. 自由記述入力
3. 政策強度選択
4. AI構造化結果
5. 関連指標・想定影響のプレビュー

### プリセット政策

MVPでは3つ。

- 消費税減税
- 子育て支援給付の拡充
- 行政DXの推進

各プリセットには以下を表示する。

- 政策名
- 1行概要
- 主な対象
- 主な論点
- 財源注意ラベル

### 自由記述入力

入力欄は1つに絞る。

例。

```text
最低賃金を全国一律で大きく引き上げる政策を検討したい
```

### 政策強度

セグメントコントロールで選ぶ。

- 小規模
- 標準
- 大規模

### AI構造化結果

AIが返す `PolicyTarget` を表示する。

- 政策名
- 政策概要
- AI生成の政策分野
- 主対象
- 主要な受益者
- 主要な負担者
- 想定財源
- 実施期間
- 関連指標
- 主な論点
- 財源上の注意

### 主な操作

- `声の分析へ進む`
- `自由記述を政策ターゲット化`

`声の分析へ進む` は、ダッシュボードの `政策ターゲットへ進む` と同じトップレベル見出し右側に置く。カード内ヘッダー右ではなく、画面全体の次アクションとして扱う。

政策ターゲットを指定するまでは、声の分析画面と政策案画面は未生成表示にする。

### 状態

- 未選択
- プリセット選択中
- 自由記述入力中
- AI構造化中
- 政策ターゲット確定済み
- 実行後ロック

## 5. 声の分析画面

### 目的

政策ターゲットに対する国民・ステークホルダーの想定反応を確認する。

### 主要領域

1. 反応サマリー
2. クラスターマップ
3. 効果軸切替
4. 代表発話
5. 階層クラスタリング

### 反応サマリー

上部に4つの比率を表示する。

- 賛成
- 反対
- 条件付き
- 無関心

補助的に表示する。

- 便益が大きいクラスター数
- 不利益が出るクラスター数
- 反発が強いクラスター数
- 実務上の懸念数

### クラスターマップ

EChartsの散布図を使う。

軸はMVPでは初期値を用意し、ユーザーが切り替えられるようにする。

- 横軸: 政策への賛否
- 縦軸: 利害の強さ

切り替え候補。

- 賛否 × 利害
- 便益 × 不利益
- 支持変化 × 感情強度
- 政策理解度 × 関心度

点の表現。

- サイズ: クラスター規模
- 色: stance
- 濃さ: 感情強度

クリック時に表示する。

- クラスター名
- 要約
- 代表発話
- 関連ペルソナ
- 関連指標

### 効果軸切替

声の分析画面では、実行前の期待効果として表示する。世代別・所得別は重要な標準ビューだが、固定表示にはしない。

切り替え候補。

- 世代別
- 所得別
- 地域別
- 職業・産業別
- 政治的立場別
- 国際関係
- 財政影響

政策によって関係が薄いビューは、タブを非表示にするか、`該当性が低い` と理由を表示する。例えば、行政DXでは世代別よりデジタル利用度や行政現場別の効果を優先できる。

#### 世代別

表示候補。

- 10代・20代
- 30代
- 40代
- 50代
- 60代
- 70代以上

#### 所得別

表示候補。

- 低所得層
- 中間層
- 高所得層

各セグメントに表示する。

- 効果スコア
- 便益レベル
- リスクレベル
- 1行理由
- 該当性

政策との関連が薄い場合は、効果スコアを空にし、`該当性が低い` と理由を表示する。

### 代表発話

カードで表示する。

- 発話者ラベル
- 複合ペルソナ
- stance
- mood
- 発話本文
- 関連指標
- 関連クラスター

特定の政治的立場、宗教・支持組織、社会運動に近いペルソナを表示する場合も、個人推定ではなく仮想反応軸として表示する。

## 6. 政策案画面

### 目的

AIが生成した政策案を確認し、チャットで調整して、実行前の最終案にする。

MVPでは「政策案」と「政策分析チャット」を1画面にまとめる。画面を分けると導線が長くなるため、左に政策案、右にチャットを置く。

### レイアウト

デスクトップ。

```text
┌──────────────────────────────┬──────────────────────┐
│ 政策案プレビュー              │ 政策分析チャット       │
│                              │                      │
│ 実施内容                      │ 質問入力              │
│ 財源案・警告                  │ AI回答履歴            │
│ 期待効果                      │                      │
│ 世代別・所得別効果            │                      │
│ クラスター別便益・不利益      │                      │
│ 国際関係への影響              │                      │
└──────────────────────────────┴──────────────────────┘
```

モバイル。

```text
政策案
↓
チャット
```

### 政策案プレビュー

表示する。

- 政策名
- 概要
- 対象者
- 実施手段
- 実施コスト
- 財源案
- 財源警告
- 短期効果
- 世代別の期待効果
- 所得別の期待効果
- 便益クラスター
- 不利益クラスター
- 反発クラスター
- 国際関係への影響
- 実装リスク
- 説明責任上の論点

効果表示は固定ブロックにせず、以下の切り替えビューで表示する。

- 世代別
- 所得別
- 地域別
- 産業別
- クラスター別
- 国際関係
- 財政・実装

初期表示は、AIが政策内容から選んだ推奨ビューにする。消費税減税なら `所得別` と `世代別`、行政DXなら `行政現場` と `デジタル利用度`、防衛・通商系なら `国際関係` を優先する。

### 政策分析チャット

初期の入力例をボタン化する。

- どの世代に効果が大きい？
- 所得別の不利益は？
- 財源を変えると反応は？
- 反発が強いクラスターは？
- 国際関係への影響は？
- 無関心層に届く説明は？

チャット回答には、参照したクラスターと指標を明示する。

### 主な操作

- `政策案を生成`
- `チャットで修正`
- `政策を実行して結果を見る`

`政策を実行して結果を見る` は、ダッシュボードと同じトップレベル見出し右側に置く。政策案カード内の下部やカードヘッダー内には重複配置しない。

### 実行前チェック

実行ボタンの近くに表示する。

- 政策実行は1回のみ
- 実行後は政策案をロック
- 財源制約は警告として扱う

## 7. 実行結果画面

### 目的

政策実行後の短期結果、属性別影響、財源・実務上の副作用を詳細レポートとして確認する。

### 主要領域

1. 実行結果サマリー
2. 短期指標変化
3. 効果軸別の短期効果
4. クラスター別影響
5. 国際関係クラスター別反応
6. 政策実行レポート

### 実行結果サマリー

上部に表示する。

- 政策名
- 実行日時
- 短期評価
- 主な改善
- 主な副作用
- 財源上の懸念

### 短期指標変化

増減カードまたは棒グラフで表示する。

- 家計可処分所得
- 税負担感
- 財政余力
- 社会保障持続性
- 政策納得度
- 行政現場負荷
- 国際信頼度

政策に関連する指標を優先表示し、関係が薄い指標は折りたたむ。

### 効果軸別の短期効果

声の分析画面と同じ `SegmentEffect` を使う。

表示。

- 効果スコア
- 便益レベル
- リスクレベル
- 理由
- `not_applicable` の場合は数値ではなく説明文

切り替え候補。

- 世代別
- 所得別
- 地域別
- 産業別
- クラスター別
- 国際関係
- 財政・実装

政策と関係があるビューだけを優先表示する。関係が薄いビューを開いた場合は、空のグラフではなく、なぜ該当性が低いかを説明する。

### クラスター別影響

テーブルまたはコンパクトカードで表示する。

- クラスター名
- 便益
- 不利益
- 納得度
- 支持変化
- 代表的な反応

### 国際関係クラスター別反応

表示する。

- アメリカ
- EU
- 中国
- アジア
- グローバルサウス
- 国際機関

各クラスターに表示する。

- 反応
- 関係スコア変化
- 通商・安全保障・サプライチェーンへの影響
- 外交説明コスト

### 長期影響

MVPでは一旦非表示にする。

後で復活する可能性があるため、内部データや表示ブロックは残してよい。ただし通常画面には `長期予想`、`長期リスク`、`長期的に観測すべき指標` といった欄を表示しない。

### 政策実行レポート

学園版の年度末レポートをベースに、国版政策向けの詳細レポートとして表示する。

- 全体のまとめ
- 影響の大きい分野における詳細な影響分析
- 影響の大きい指標の可視化
- 属性別反応の可視化
- 財源・社会保障・産業・実務面の副作用

### 主な操作

- `新しい政策で開始`
- `JSONエクスポート`
- `レポートをコピー`

## 8. 保存・AI設定モーダル

既存モーダルを流用する。

### 保存

- ブラウザ保存
- JSONエクスポート
- JSONインポート
- ブラウザ保存削除

保存名やファイル名は国版に変更する。

```text
national-policy-simulator-YYYY-MM-DD.json
```

### AI設定

既存Providerを流用する。

- 固定サンプル
- Codex App Server
- OpenAI Responses
- OpenAI互換 Chat Completions

## 9. UI密度と見た目の方針

国版政策シミュレーターは、業務系ダッシュボードに近い。

避けるもの。

- 大きすぎるヒーロー
- マーケティングLP風の見出し
- 装飾的なカード過多
- 単色に寄りすぎた配色
- 重要データがスクロール下に埋もれる構成

目指すもの。

- 密度はやや高め
- 情報はカテゴリ別に整理
- 政策判断に必要な情報を初期表示
- チャートは説明より比較を優先
- 指標・効果は固定表示にせず、政策ごとの推奨ビューを初期表示する
- 操作ボタンは少なく、次に進む導線を明確にする
- 国際関係、世代別、所得別などの比較軸は視覚的に並べる

### 配色

既存の深緑系を残してよいが、国版では以下を追加する。

- 社会指標: 青系
- 経済・財政: 緑系
- リスク・反発: 赤系
- 注意・財源懸念: 黄系
- 国際関係: 紫に寄りすぎない紺・シアン系

### コンポーネント

- 指標カード
- 分析ビュー切替
- セグメント効果バー
- クラスター散布図
- 代表発話カード
- 政策案プレビュー
- 警告パネル
- チャットパネル
- 実行結果レポート

## 10. 実装時の置き換え単位

### app state

既存。

```text
issues
issueSelectionChat
policy
lastSimulationResult
annualReport
```

国版。

```text
policyTargets
policyTarget
policyAnalysisChat
policyDraft
executionResult
```

### activeView

既存。

```text
dashboard
voices
issues
policy
result
```

国版。

```text
dashboard
target
voices
policy
result
```

`issues` は互換性のため一時的に残してもよいが、画面ラベルは `政策ターゲット` にする。

### 初期実装の優先順

1. ナビと画面ラベルを国版へ変更
2. 政策ターゲット画面の静的UIを作る
3. ダッシュボードの指標カテゴリを国版へ変更
4. 声の分析に世代別・所得別効果を追加
5. 政策案画面にチャットと実行前チェックを統合
6. 実行結果画面を政策実行向けに変更
7. 保存ファイル名とDB名を国版へ変更

## 11. MVP画面完了条件

- ユーザーが迷わず `政策ターゲット -> 声の分析 -> 政策案 -> 実行結果` に進める
- 政策実行が1回だけであることが画面上で分かる
- 世代別・所得別効果が、声の分析、政策案、実行結果の3箇所で一貫して見える
- 世代や所得との関連が薄い政策では、無理な数値ではなく該当性が低い理由が出る
- 国際関係クラスターの反応がダッシュボードと実行結果で見える
- 財源上の懸念が政策案と実行結果で見える
- 長期影響欄はMVPでは表示されない
